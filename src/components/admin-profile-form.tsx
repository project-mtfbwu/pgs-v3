"use client";
import { useState } from "react";
export function AdminProfileForm({displayName,email}:{displayName:string;email:string}){const [message,setMessage]=useState("");async function save(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const data=new FormData(event.currentTarget);const response=await fetch("/api/admin/profile",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({display_name:data.get("display_name")})});const result=await response.json();if(!response.ok)return setMessage(result.message??"Unable to save.");setMessage("Profile updated and audited.");}return <form className="ops-card ops-profile-form" onSubmit={save}><label>Auth email<input value={email} readOnly/></label><label>Staff display name<input name="display_name" required defaultValue={displayName} maxLength={255}/></label><button className="ops-primary">Save profile</button><p role="status">{message}</p><a href="/change_password">Change password through Supabase Auth →</a></form>}
