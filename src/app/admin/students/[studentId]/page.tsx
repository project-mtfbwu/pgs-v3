import { notFound } from "next/navigation";
import { AdminPageHeader } from "@/components/admin-page-header";
import { StaffKanbanBoard } from "@/components/staff-kanban-board";
import { StaffWorkspaceControls } from "@/components/staff-workspace-controls";
import { loadPremiumWorkspace,WorkspaceAccessError } from "@/lib/premium-workspace";
import { requireStudentViewer } from "@/lib/student-access";
import { createSupabaseServerClient } from "@/lib/supabase/server";
export default async function AdminStudentWorkspace({params}:{params:Promise<{studentId:string}>}){const {studentId}=await params;let actor,workspace;try{actor=await requireStudentViewer(studentId,{route:"/admin/students/[studentId]"});workspace=await loadPremiumWorkspace(studentId);}catch(error){if(error instanceof WorkspaceAccessError)notFound();throw error;}const supabase=await createSupabaseServerClient();const {data:universities}=await supabase.from("universities").select("id,name").order("name").limit(300);return <main className="ops-page"><AdminPageHeader eyebrow="Student workspace" title={workspace.profile?.full_name||"Premium student"} description={`Authorized as ${actor.kind.replaceAll("_"," ")}. Every change below uses the student's shared relational workspace.`}/><section className="ops-metrics"><div><span>Pathway</span><strong>{workspace.premiumProfile?.pathway_label||"Not set"}</strong></div><div><span>Documents</span><strong>{workspace.requirements.length}</strong></div><div><span>Active alerts</span><strong>{workspace.alerts.length}</strong></div><div><span>Tasks</span><strong>{workspace.tasks.length}</strong></div></section><StaffWorkspaceControls studentId={studentId} columns={workspace.columns} requirements={workspace.requirements} universityOptions={universities??[]} comments={workspace.comments} alerts={workspace.alerts} reviews={workspace.reviews} notes={workspace.notes} selections={workspace.universities} premiumProfile={workspace.premiumProfile}/><section className="ops-card"><h2>One shared student board</h2><StaffKanbanBoard studentId={studentId} columns={workspace.columns} tasks={workspace.tasks}/></section><section className="staff-workspace-data"><div><h2>Comments & alerts</h2>{workspace.comments.map((comment)=><article key={comment.id}><span>{comment.parent_id?"Reply":"Comment"}</span><p>{comment.body}</p></article>)}{workspace.alerts.map((alert)=><article key={alert.id}><span>{alert.severity}</span><p>{alert.alert_text}</p></article>)}</div><div><h2>Reviews</h2>{workspace.reviews.map((item)=><article key={item.id}><strong>{item.title}</strong><span>{item.status}</span></article>)}</div><div><h2>Notes</h2>{workspace.notes.map((note)=><article key={note.id}><span>{note.visibility.replaceAll("_"," ")}</span><p>{note.body}</p></article>)}</div><div><h2>Documents</h2>{workspace.requirements.map((item)=><article key={item.id}><strong>{item.document_type}</strong><span>{item.status}</span></article>)}</div></section></main>}
