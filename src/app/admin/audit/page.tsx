import { AdminPageHeader } from "@/components/admin-page-header";
import { requireStaffPermission } from "@/lib/staff-auth";
import { createSupabaseServerClient } from "@/lib/supabase/server";
export default async function AuditPage({searchParams}:{searchParams:Promise<{domain?:string}>}){await requireStaffPermission("audit.read");const filters=await searchParams;const supabase=await createSupabaseServerClient();let query=supabase.from("admin_audit_logs").select("id,actor_id,action,domain,entity_type,entity_id,target_user_id,reason,created_at").order("created_at",{ascending:false}).limit(250);if(filters.domain)query=query.eq("domain",filters.domain);const [{data:adminLogs},{data:premiumLogs}]=await Promise.all([query,supabase.from("premium_audit_logs").select("id,actor_id,student_id,action,entity_type,entity_id,reason,created_at").order("created_at",{ascending:false}).limit(100)]);const events=[...(adminLogs??[]).map((row)=>({...row,subject:row.target_user_id??row.entity_id})),...(premiumLogs??[]).map((row)=>({...row,domain:"premium",subject:row.student_id??row.entity_id}))].sort((a,b)=>new Date(b.created_at).getTime()-new Date(a.created_at).getTime());return <main className="ops-page"><AdminPageHeader eyebrow="Audit" title="Activity and security audit" description="Append-oriented actor, action, subject, reason, and timestamp evidence across staff, CMS, catalog, leads, Premium, and student operations."/><div className="ops-toolbar"><form method="get"><select name="domain" defaultValue={filters.domain??""}><option value="">All domains</option><option>staff</option><option>catalog</option><option>content</option><option>cms</option><option>leads</option><option>settings</option></select><button>Filter</button></form></div><div className="ops-table-wrap"><table><thead><tr><th>Time</th><th>Domain</th><th>Action</th><th>Entity</th><th>Actor</th><th>Subject / reason</th></tr></thead><tbody>{events.map((event)=><tr key={`${event.domain}-${event.id}`}><td>{new Date(event.created_at).toLocaleString("en-GB")}</td><td><span className="ops-badge">{event.domain}</span></td><td>{event.action}</td><td>{event.entity_type}<br/><code>{event.entity_id}</code></td><td><code>{event.actor_id??"system"}</code></td><td><code>{event.subject??"—"}</code><br/>{event.reason??""}</td></tr>)}</tbody></table></div></main>}

